---
headline: Open bell · Robinhood unreachable again (every scheduled run since last evening) · action needed: reconnect the connector
---
# Trading Run Report

## Mode
Report-only, scheduled opening-bell run (9:35 AM ET). No orders placed.

## Account snapshot
Unavailable. The Robinhood connector has now failed on every scheduled run since yesterday evening (closing-bell test, 8:00 AM pre-market, and this 9:35 open-bell run). Last known state (Jul 20, 4:39 PM ET): Agentic account ••••7647, $10.95 cash, no positions, no open orders.

## Analysis
No chain data, so no candidate plays can be built this run. The failure pattern has changed from intermittent (self-healing within the same session yesterday afternoon) to persistent across scheduled wake-ups — that points at an expired grant on the scheduled-session side rather than server flakiness.

## Actions taken
None.

## Proposals
None this run — no data. The moonshot candidate list resumes the first run after the connection returns.

## Follow-ups for the human
1. **Reconnect Robinhood now:** claude.ai → Settings → Connectors → Robinhood → disconnect, then connect and log in again. This is the firm recommendation after three consecutive failures.
2. **Or use the dashboard as the workaround:** the "Find fresh options" button and Refresh run through your own browser session — independent of the scheduled runs. If those also fail with a "reconnect" message, that confirms the grant is dead.
3. The closing-bell run (4:05 PM ET) will retry automatically either way.
